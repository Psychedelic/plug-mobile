import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  content: {
    paddingBottom: 20,
  },
  buttonContainer: {
    flexDirection: 'row',
    paddingHorizontal: 20,
    marginTop: 30,
  },
});
