import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  toastStyle: {
    top: 48,
    left: -3,
  },
  toastPointerStyle: {
    top: -4,
  },
});
