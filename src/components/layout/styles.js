import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  column: {
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
  },
  row: {
    display: 'flex',
    flexDirection: 'row',
    flex: 1,
  },
});
